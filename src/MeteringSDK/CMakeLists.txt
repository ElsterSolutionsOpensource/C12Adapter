CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(MeteringSDK)

SET(INSIDE_METERINGSDK TRUE)

INCLUDE(CMake/Modules/MeteringSDKInternal.cmake)

FOREACH(_COMPONENT ${METERINGSDK_COMPONENTS})
   ADD_SUBDIRECTORY(${_COMPONENT})
ENDFOREACH()
